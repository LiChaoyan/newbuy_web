<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xoqao.web.dao.EmployerMapper">

    <!--添加店员信息-->
    <insert id="insertEmployer">
        <selectKey resultType="java.lang.Integer" keyProperty="eid" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO employer (des,email,gender,identity,logo,name,password,phone,sid,username) VALUES (#{employer.des},#{employer.email},#{employer.gender},#{employer.identity},#{employer.logo},#{employer.name},#{employer.password},#{employer.phone},#{employer.sid},#{employer.username})
    </insert>


</mapper>